### 2026년 1월 8일 카테고리 및 속성에 대한 논의 
```
# 요구사항 점검
## 전제조건 : 모든 데이터는 삭제 혹은 해제 등 기능에 대해선 논리 삭제
## 추후 개발 예정 : 추후 논리 삭제 정보와 연계하여 이력관리용으로 활용됨
## 필요시 검토사항 : 논리 삭제를 위한 개념 정의 (추후 개발을 목적으로하여 개념 정의를 안함) , 개념 정의가 없어도 현재 개발 모델에 문제가 없을것으로 판단함

#문제점 확인 
1. 속성이 보드에 연결되는 경로 B 보드에서 속성이 직접 연결되는 구조 (현재 설계상 맞지 않음) 
  └ 모든 속성은 카테고리를 통해 연결되며 관리되어야 함... 
  └ 예외 속성은 기본속성 , 글로벌속성 , 매니저 속성 ,만약 이 경우가 경로 B에 해당한다면 필요함
  └ 기본속성은 무조건 독립적 
  └ 글로벌 속성도 독립적 
  └ 매니저 속성은 제한을 가진 독립적 (매니저 role을 부여한 사용자가 속한 부서 및 하위 부서사용자들만 사용할수 있음)
2. 속성의 결정 기준
  └ 사용자 편의를 위해 보드에 귀속시킨 카테고리 및 속성을 우선 제시하는 개념 
  └ 사용자가 선택할수 있는 기준 상속받은 속성 또는 편의에 혹은 필요에 의해 변경한 카테고리 속성(사용자 속성)
  └ 변경 불가 속성은 기본속성만 해당 글로벌 속성 및 매니저 속성도 필요에 따라 상속되었지만 해제후 변경 가능 
3. 카테고리 없이 속성만 선택 가능 
  └ 카테고리에 귀속되지 않는 속성은 기본속성 , 글로벌 속성 , 매니저 속성만 해당
  └ 사용자에 의해 생성된 속성은 무조건 카테고리에 귀속되며 카테고리를 통해서만 공급 받을수 있음 
4. 값의 여부 (스키마와 별개로 설명함)
  └ 속성이 부여되면 해당 속성은 값의 부여 여부와 관계없이 귀속이 되어야 하기에 정의가 필수
  └ 속성을 해제 또는 변경 (변경 역시 기존 속성 해제후 신규 부여로 판단됨) 시 신규 속성 부여 와 해제 정의 구간이 있어야함 
  └ 그런 의미에서 TB_ITEM_PROPERTY의 정의 구조는 적절하지만 해제하는 구간이나 해제후 이력관리를 위한 연결 구조 등에 대한 정의가 없어 일부 취약해 보임
  └ 정의상 속성의 배정과 해제시 해제된 속성은 이력으로 관리되어야 하기에 논리 해제가 필요하며 동일 속성의 재배정을 고려해 id 관리는 필수 임 
  └ 너무 많은 부분을 개발해야 하기에 아직 이력관리는 논의 대상으로 지정하지 않았음 (추후 개발 목표)

5.속성의 가용범위 정의 
  └ 해당 내용은 위 내용으로 (1~4) 충분히 정의 됨 (정의가 어렵거나 정의가 모호할경우 다시 질문 바람)

# 질의 응답
1. 업무 속성 "선택됨"상태 의미와 속성의 "가용범위"정의 , 카테고리의 역활 이 세가지 질문은 스키마 구조때문에 혼동이 있는듯함

2. 위에서 설명한대로 속성은 4종류로 정의됨
- 기본 속성 : 필수 요소 , 변경불가 , 업무 생성시 무조건 부여
- 글로벌 속성 : 선택 요소 , 변경 가능 , 관리자에 의해서만 생성 가능 (ROLE : ADMIN)
- 매니저 속성 : 선택 요소 , 변경 가능 , 매니저 또는 관라지에 의해서만 생성 가능 (ROLE : ADMIN , MANAGER)
- 개인 속성 : 선택 요소 , 변경 가능 , 사용자에의해 생성 (ROLE : ADMIN , MANAGER 제외한 나머지)

3. 카테고리에 귀속되는 속성 : 개인 속성 

# 부여 방식 
1. 보드 : 기본속성, 글로벌 속성(선택) , 매니저 속성(선택) , 개인속성(선택, 지정된 카테고리 기준)
  └ 보드에 부여된 속성은 값을 가지지 않음 부여된 정의만 보유함
  └ 업무 생성시 업무에 정의할 속성을 빠르게 부여하기 위한 목적 (상속 용도)
2. 업무 : 보드로부터 상속받은 모든 속성 
  └ 정의된 속성을 바탕으로 값을 입력 받고 관리 역활을 하게 됨

# 주의 사항
1. 부여 방식의 주의 사항 : 보드 및 업무는 모두 사전 정의된 속성이 있음 다만, 생성 과정에서 정의 되거나 상속된 속성을 필요에 의해 재정의 가능하도록 하고자함
2. 생성 및 등록시 재정의 사유 : 이렇게 안할경우 필요에 의해 여러가지 중복된 카테고리가 무한정 생성되는 문제를 야기할수 있음 
3. 특수 상황 1 : 카테고리 공유 문제 역시 동일 구조의 카테고리에 대한 무한 생성을 막기 위함 
4. 특수 상황 2 : 카테고리에 대한 업무 이관 문제는 기존 정의된 카테고리와 배정된 보드 혹은 업무에 대해 사용자 탈퇴 혹은 삭제로 인한 카테고리 정의 상실 문제를 해결하기 위함 

# 다음 개발 방향 
1. 업무에 정의된 속성의 변경 (배정,해제) 또는 업무 등록후 입력되는 값의 변화에 대한 이력관리 
2. 템플릿 정의시 카테고리 지정 기능 (업무 생성시 템플릿 호출에 유용할것으로 판단)

#미래 개발 방향 
1. 업무에 하위 업무 생성 기능 
  └ 예를 들어 A 라는 보드에 A-1 이라는 업무를 생성했을경우 A-1 이란 업무 진행시 파생되는 소단위 업무에 대해 A-1에 연관 구성을 위해 하위 업무로 A-1-1 로 생성 테이블 표현에선 아래오 같이 표현 예상 

┌──────────────────────────
│업무 (A-1)................ 속성 카테고리 (CAT-1)
├──────────────────────────
│ └ 업무 (A-1-1) ......... 속성 카테고리 (CAT-2)
│ └ 업무 (A-1-2) ......... 속성 카테고리 (CAT-1)
│   └ 업무 (A-1-2-1) ..... 속성 카테고리 (CAT-3)               (A-1-2) 에서 파생된 속성 

이 개념은 업무가 생성되었을 경우 파생해서 회의를 하거나 내부 기능을 추가하기위한 기능 생성을 별도로 하거나 타 부서와 협을 위한 업무 분할을 하거나 할경우 활용됨 
즉, 하위 업무 개념이 없을경우 연관 업무가 연관성 표현이 어려워 복작해지거나 관리가 안되는 현상이 나타날수 있음

2. 업무간의 연관 플로우 생성 (최종 목표)
  └ 여러 업무 가 타 업무와 연관성을 가지는 구조를 만들고 연관 성에 따라 FLOW 생성 및 선행 업무의 완료 또는 승인 구조를 통해 하위 업무로 자연 배당 사용자에게 생성자 및 타부서 혹은 타 사용자에게 자동 배분 부여 하는 방식으로 업무의 연속성과 흐름을 관리하는 목적을 가짐 
  └ 해당 개념은 비지니스 플로우나 워크플로우 개념과 비슷하며 관리와 유연함을 목적으로 함

# 관리용 메뉴 추가 
1. 업무 상태 정보 관리용 메뉴 필요 
  └ 업무내용 , 카테고리 , 담당자 ,요청일 , 시작일 , 완료일 , 마감일 , 상태
  └ 업무 상태 정보 변경시 상태정보 갱신 
  └ 지연 계산은 날짜 관련 속성 변경시 또는 업무 저장시 
  └ 지연 계산은 사용자 로그인시 보유중인 업무 특히 미종료 업무 대상으로 진행

2. 요청일(기본속성) , 시작일(추가속성) , 완료일(추가속성) , 마감일(기본속성) 에 대한 이력관리 필요 
  └ 향후 프로젝트 변경에 따라 시작일 혹은 요청일 변경될경우 변경 기간 등 표현시 활용
  └ 간트 차트 등으로 표현될경우 시작일 , 요청일 , 마감일 등이 변경된 업무의 경우 변경이력을 간트로 표현 가능한 용도등 
  └ 간트에 한정되지 않으며 변경에 따른 난이도 변경 목적 변경등 변경 사유 확인을 위함 
  └ 각 업무에 대해 속성별 값에 대해 변경시 이력 관리 목적 "업무 생성" , "최초 등록" , "변경" , "완료" 


```
---
# 수정 계획서

  문제 요약

  | #   | 문제                                       | 원인                               | 영향                        |
  |-----|--------------------------------------------|------------------------------------|-----------------------------|
  | 1   | 업무 생성 시 선택한 속성이 DB에 저장 안됨  | 값이 있는 속성만 properties에 포함 | 속성 귀속 상태 유실         |
  | 2   | 업무 수정 모달 초기값이 실제 데이터와 다름 | propertyStore 기준으로 초기화      | 생성 시 선택 ≠ 수정 시 표시 |
  | 3   | 사이드 패널과 모달 표시 불일치             | 서로 다른 데이터 소스 참조         | UI 혼란                     |

  ---
  수정 원칙 (CONCEPT_NOTE 기반)

  1. 속성이 부여되면 값 유무와 관계없이 TB_ITEM_PROPERTY에 귀속
  2. 업무의 "현재 속성 목록" = TB_ITEM_PROPERTY 기준
  3. 모든 UI는 동일한 데이터 소스(TB_ITEM_PROPERTY) 참조

  ---
  단계별 수정 계획

  1단계: 업무 생성 시 속성 저장 (프론트엔드)

  파일: NewItemInput.vue

  현재:
  for (const propId of data.propertyIds) {
    if (data.propertyDefaults[propId]) {  // 값이 있는 것만
      properties[propId] = data.propertyDefaults[propId]
    }
  }

  수정:
  for (const propId of data.propertyIds) {
    // 값이 없어도 빈 문자열로 저장 (귀속 상태 유지)
    properties[propId] = data.propertyDefaults[propId] || ''
  }

  ---
  2단계: 업무 수정 시 속성 저장 (프론트엔드)

  파일: ItemDetailPanel.vue - handlePropertySave

  현재 문제: 이미 수정했으나 동작 확인 필요

  확인 사항:
  - 선택된 속성 → 빈 문자열로 저장
  - 해제된 속성 → null로 전송하여 삭제

  ---
  3단계: 백엔드 속성 저장 로직 확인

  파일: ItemServiceImpl.java - saveItemProperties

  현재:
  if (value == null) {
      // 삭제
      itemPropertyMapper.deleteByItemIdAndPropertyId(itemId, propertyId);
      continue;
  }
  // 값이 있으면 저장
  saveSingleProperty(...)

  확인: 빈 문자열("")도 정상 저장되는지 확인

  ---
  4단계: 속성 수정 모달 초기값 수정 (핵심)

  파일: ItemDetailPanel.vue - initializePropertySelection

  현재 (문제):
  // 보드의 모든 속성으로 초기화 (잘못됨)
  const boardPropertyIds = propertyStore.sortedProperties.map(p => p.propertyId)
  selectedPropertyIds.value = boardPropertyIds

  수정:
  // 아이템에 실제 저장된 속성으로 초기화
  const itemPropertyIds = itemData.properties?.map(p => p.propertyId) || []
  selectedPropertyIds.value = itemPropertyIds

  ---
  5단계: 사이드 패널 속성 표시 통일

  파일: ItemForm.vue - customProperties

  현재: item.properties 기반 (정상)

  확인: TB_ITEM_PROPERTY에 저장된 속성만 표시하는지 확인

  ---
  수정 순서

  ┌─────────────────────────────────────────────────────────────┐
  │ 1. NewItemInput.vue 수정 (생성 시 모든 선택 속성 저장)        │
  ├─────────────────────────────────────────────────────────────┤
  │ 2. 백엔드 saveItemProperties 동작 확인                       │
  ├─────────────────────────────────────────────────────────────┤
  │ 3. ItemDetailPanel.vue initializePropertySelection 수정     │
  │    (모달 초기값 = item.properties 기준)                      │
  ├─────────────────────────────────────────────────────────────┤
  │ 4. 통합 테스트                                               │
  │    - 업무 생성 → DB 확인 → 모달 열기 → 표시 확인             │
  └─────────────────────────────────────────────────────────────┘

  ---
  예상 결과

  | 시나리오                   | 수정 전             | 수정 후                 |
  |----------------------------|---------------------|-------------------------|
  | 업무 생성 시 4개 속성 선택 | DB에 1개만 저장     | DB에 4개 모두 저장      |
  | 속성 수정 모달 열기        | 보드 전체 속성 표시 | 실제 저장된 속성만 표시 |
  | 사이드 패널 vs 모달        | 불일치              | 일치                    |

  ---

    테스트 1: 업무 생성 시 속성 저장

  1. 업무 등록 모달에서 속성 4개 선택 (값 입력 없이)
  2. 업무 등록 완료
  3. 검증: DB에 4개 속성 레코드가 생성되었는지 확인
  SELECT * FROM TB_ITEM_PROPERTY WHERE ITEM_ID = [새 업무 ID];

  테스트 2: 사이드 패널 표시 확인

  1. 생성한 업무 클릭하여 사이드 패널 열기
  2. 검증: "추가 속성" 섹션에 선택한 4개 속성이 표시되는지 확인

  테스트 3: 속성 수정 모달 초기값

  1. "속성수정" 버튼 클릭
  2. 검증: 모달에서 4개 속성만 체크되어 있는지 확인 (보드 전체 속성이 아님)

  테스트 4: 속성 해제/추가 동작

  1. 모달에서 기존 속성 1개 해제, 새 속성 1개 추가
  2. 저장 후 다시 모달 열기
  3. 검증: 변경된 속성 목록이 정확히 반영되는지 확인

  ---
  테스트 진행 후 결과를 알려주시면 최종 완료 처리하겠습니다. 